<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Firebase / Analytics / Crashlytics need network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- REL-5: the advertising ID is never used. firebase-analytics declares AD_ID in its own library
         manifest, so it has to be stripped at merge time - deleting this line would not be enough. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />

    <application>
        <!-- REL-5 / UK GDPR: Firebase Analytics and Crashlytics start from their own ContentProviders
             before any C# runs, so collection starts disabled in the manifest and analytics is enabled
             explicitly from code only after consent.

             Deliberately NOT firebase_analytics_collection_deactivated: that flag is permanent and cannot
             be overridden at runtime. -->
        <meta-data android:name="firebase_analytics_collection_enabled" android:value="false" />
        <meta-data android:name="firebase_crashlytics_collection_enabled" android:value="false" />
        <meta-data android:name="google_analytics_adid_collection_enabled" android:value="false" />
        <meta-data android:name="google_analytics_ssaid_collection_enabled" android:value="false" />

        <!-- Application Entry Point = Activity (Player Settings → Android → Other Settings).
             Если когда-то переключишься на GameActivity — закомментируй этот блок и раскомментируй
             блок с UnityPlayerGameActivity / @style/BaseUnityGameActivityTheme ниже. -->
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:theme="@style/UnityThemeSelector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>
        <!--
        <activity android:name="com.unity3d.player.UnityPlayerGameActivity"
                  android:theme="@style/BaseUnityGameActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
            <meta-data android:name="android.app.lib_name" android:value="game" />
        </activity>
        -->
    </application>
</manifest>
