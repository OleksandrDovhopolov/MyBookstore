namespace Game.Quest
{
    /// <summary>
    /// Placeholder so the (still empty) Game.Quest runtime assembly compiles to a real DLL that other
    /// assemblies can reference/resolve. Replaced by real services in the implementation stage.
    /// </summary>
    internal static class QuestAssemblyMarker
    {
    }
}
