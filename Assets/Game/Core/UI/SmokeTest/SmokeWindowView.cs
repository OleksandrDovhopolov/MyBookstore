using UnityEngine;

namespace Game.UI.SmokeTest
{
    // Generic view used by all three smoke-test windows. The prefab just needs
    // a Canvas + CanvasGroup + something visible (Image with a TMP label).
    public sealed class SmokeWindowView : WindowView { }
}
