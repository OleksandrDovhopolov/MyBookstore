namespace UIShared
{
    /// <summary>Concrete tab bar over <see cref="TabType"/> — used by the Shop and Inventory windows.</summary>
    public sealed class GameTabBar : TabBar<TabType>
    {
    }
}
