namespace UIShared
{
    /// <summary>Concrete tab button over <see cref="TabType"/> — used by the Shop and Inventory windows.</summary>
    public sealed class GameTabButton : TabButton<TabType>
    {
    }
}
